export enum Role {
  ADMIN = "admin",
  USER = "user",
}

export enum ArticleEnum {
  review = "review",
  testimonial = "testimonial",
}

export enum orderStatus {
  pending = "pending",
  completed = "completed",
  failed = "failed",
  cancelled = "cancelled",
}
